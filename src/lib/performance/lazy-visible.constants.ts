/** Default preload band before the sentinel enters the viewport. */
export const LAZY_VISIBLE_DEFAULT_ROOT_MARGIN = '320px 0px';

/** Wider preload for tall below-the-fold home sections (product carousels). */
export const LAZY_VISIBLE_HOME_SECTIONS_ROOT_MARGIN = '480px 0px';

/** Preload footer before the user reaches the page bottom. */
export const LAZY_VISIBLE_FOOTER_ROOT_MARGIN = '600px 0px';

/** IntersectionObserver threshold — any pixel visible triggers mount. */
export const LAZY_VISIBLE_OBSERVER_THRESHOLD = 0;
