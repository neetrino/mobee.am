/**
 * Dismiss / remove controls: Mobee blue hover (avoid red on X-style actions).
 */

/** Bordered circle (cart / wishlist line remove, mobile drawer close). */
export const DISMISS_ROUND_BUTTON_HOVER_CLASS =
  'hover:border-admin-300 hover:bg-admin-50 hover:text-admin-600';
