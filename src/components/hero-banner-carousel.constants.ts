/** Hero promo carousel — auto-advance interval (ms). */
export const HERO_BANNER_AUTO_INTERVAL_MS = 2500;

/** Slide transition duration (ms). */
export const HERO_BANNER_TRANSITION_MS = 500;

/** Horizontal gap between peek slides (px). */
export const HERO_BANNER_SLIDE_GAP_PX = 12;

/** Banner intrinsic aspect ratio — slightly taller for a larger hero presence. */
export const HERO_BANNER_ASPECT_RATIO = 1024 / 460;
