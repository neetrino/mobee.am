/**
 * Compare / wishlist / cart count badges — mobee cyan (`#2db2ff`).
 */

/** Corner pill on secondary bar icons (desktop navbar). */
export const HEADER_NAV_ICON_COUNT_OVERLAY_BADGE_CLASS =
  'absolute -right-0.5 -top-0.5 flex h-3.5 min-w-3.5 items-center justify-center rounded-full bg-[#2db2ff] px-0.5 text-[9px] font-normal leading-none text-white';

/** Inline pill in mobile drawer row (e.g. compare link). */
export const HEADER_NAV_COUNT_INLINE_BADGE_CLASS =
  'rounded-full bg-[#2db2ff] px-2 py-0.5 text-xs font-semibold text-white';
