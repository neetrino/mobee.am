/**
 * Empty wishlist illustration — exported from Figma mobee-new (183:1986).
 * Layout numbers live in `empty-wishlist.tsx` Tailwind classes.
 */
export const EMPTY_WISHLIST_ILLUSTRATION_PATH = '/images/empty-wishlist-illustration.png';

/** Source asset dimensions after export optimization (see `/public/images`). */
export const EMPTY_WISHLIST_ILLUSTRATION_WIDTH_PX = 380;
export const EMPTY_WISHLIST_ILLUSTRATION_HEIGHT_PX = 570;

/** Figma frame 256×247; slightly smaller below `md` for phones. */
export const EMPTY_WISHLIST_FRAME_CLASS =
  'relative h-[230px] w-[240px] shrink-0 md:h-[247px] md:w-[256px]';

export const EMPTY_WISHLIST_IMAGE_SIZES = '(max-width: 767px) 240px, 256px';
