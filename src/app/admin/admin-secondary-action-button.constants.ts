/**
 * Use with `<Button type="button" variant="outline" className={ADMIN_SECONDARY_OUTLINE_BUTTON_EXTRA_CLASS} />`
 * for cancel / clear actions on `/supersudo/*` so the control reads clearly on light gray page chrome.
 */
export const ADMIN_SECONDARY_OUTLINE_BUTTON_EXTRA_CLASS =
  'border-gray-300 bg-gray-100 text-gray-800 shadow-sm hover:bg-gray-200/90';
