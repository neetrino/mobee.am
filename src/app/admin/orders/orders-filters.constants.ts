/**
 * Class list for the three status `<select>` rows on `/supersudo/orders`.
 * Chevron: `globals.css` @layer utilities `.orders-filter-select` (`background-position` nudge).
 */
export const ORDERS_FILTER_SELECT_CLASS =
  'orders-filter-select min-w-0 rounded-supersudo border border-gray-300 bg-white px-3 py-2 pr-10 text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-admin';
