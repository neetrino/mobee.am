/**
 * Checkbox «Օնլայն վճարումներ» on `/supersudo/settings` — styles in `globals.css`
 * (`.admin-settings-online-payments-checkbox`) so `url(data:...)` is not parsed as a webpack module.
 */
export const ADMIN_SETTINGS_ONLINE_PAYMENTS_CHECKBOX_CLASS = 'admin-settings-online-payments-checkbox';
