/**
 * Primary admin actions that use the panel accent (theme `admin`, #2DB2FF).
 * Overrides @shop/ui `variant="primary"` gray — e.g. discount «պահել», promocode create.
 */
export const ADMIN_DISCOUNT_SAVE_BUTTON_CLASS =
  '!bg-admin !text-white hover:!brightness-95 active:!brightness-90 focus:!ring-admin';
